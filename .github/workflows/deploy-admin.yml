name: Deploy Admin Portal
on:
  push:
    branches: [main]
    paths: ['admin-portal/**']
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: cd admin-portal && yarn install
      - run: cd admin-portal && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_AEROTECH_ADMIN }}
          channel: live
          projectId: aerotech-ops